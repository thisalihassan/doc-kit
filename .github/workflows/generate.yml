name: Generate and Compare Docs

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.push.outputs.sha || steps.pr.outputs.sha }}
      base-run: ${{ steps.main.outputs.run_id }}
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      # If we are running from the main branch (a non-pull_request event), we
      # want the latest SHA from nodejs/node
      - id: push
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          SHA=$(git ls-remote https://github.com/nodejs/node.git HEAD | awk '{print $1}')
          echo "$SHA" > commit
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"

      - name: Upload metadata artifact
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: commit
          path: commit

      # If we are running from a PR (a pull_request event), we want the SHA
      # used by the matching `main` push run in this repository. Forks have
      # their own workflow database IDs, so look up the workflow by file name.
      - name: Get latest `main` run
        if: ${{ github.event_name == 'pull_request' }}
        id: main
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          ID=$(
            gh run list \
              --repo "$GITHUB_REPOSITORY" \
              -c "$BASE_SHA" \
              --workflow generate.yml \
              -L 1 \
              --json databaseId \
              --jq '.[0].databaseId // ""'
          )
          echo "run_id=$ID" >> "$GITHUB_OUTPUT"

          if [ -n "$ID" ]; then
            echo "Using base run: $ID"
          else
            echo "No matching main run found; comparison steps will be skipped."
          fi

      - name: Download metadata artifact
        if: ${{ github.event_name == 'pull_request' && steps.main.outputs.run_id }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: commit
          run-id: ${{ steps.main.outputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - id: pr
        if: ${{ github.event_name == 'pull_request' }}
        run: |
          if [ -f commit ]; then
            SHA=$(cat commit)
          else
            SHA=$(git ls-remote https://github.com/nodejs/node.git HEAD | awk '{print $1}')
          fi
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"

  generate:
    runs-on: ubuntu-latest
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: man-page
            input: './node/doc/api/cli.md'

          - target: addon-verify
            input: './node/doc/api/addons.md'

          - target: api-links
            input: './node/lib/*.js'
            compare: object-assertion

          - target: orama-db
            input: './node/doc/api/*.md'
            compare: file-size

          - target: json-simple
            input: './node/doc/api/*.md'

          - target: legacy-json
            input: './node/doc/api/*.md'
            compare: object-assertion

          - target: legacy-html
            input: './node/doc/api/*.md'
            compare: file-size

          - target: web
            input: './node/doc/api/*.md'
            compare: file-size

          - target: llms-txt
            input: './node/doc/api/*.md'
            compare: file-size
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@fa2e9d605c4eeb9fcad4c99c224cee0c6c7f3594 # v2.16.0
        with:
          egress-policy: audit

      - name: Git Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Git Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          repository: nodejs/node
          ref: ${{ needs.prepare.outputs.sha }}
          sparse-checkout: |
            doc
            lib
            .
          path: node

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create output directory
        run: mkdir -p out/${{ matrix.target }}

      - name: Generate ${{ matrix.target }}
        run: |
          node bin/cli.mjs generate \
            -t ${{ matrix.target }} \
            -i "${{ matrix.input }}" \
            -o out \
            -c ./node/CHANGELOG.md \
            --type-map "./node/doc/type-map.json" \
            --index ./node/doc/api/index.md \
            --log-level debug

      - name: Download base branch artifact
        if: ${{ matrix.compare && needs.prepare.outputs.base-run }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ matrix.target }}
          path: base
          run-id: ${{ needs.prepare.outputs.base-run }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Compare to base branch
        if: ${{ matrix.compare && needs.prepare.outputs.base-run }}
        env:
          GENERATOR: ${{ matrix.target }}
        run: |
          node scripts/comparators/${{ matrix.compare }}.mjs > out/comparison.txt

      - name: Validate anchor links
        if: ${{ matrix.target == 'legacy-html' }}
        env:
          GENERATOR: ${{ matrix.target }}
        shell: bash
        run: |
          set -o pipefail
          node scripts/comparators/validate-links.mjs | tee -a out/comparison.txt

      - name: Upload ${{ matrix.target }} artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: ${{ matrix.target }}
          path: out
          if-no-files-found: ignore
